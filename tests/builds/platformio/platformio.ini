
[platformio]
extra_configs = ~/.fw-context/platformio-shared.ini
; PlatformIO test project for fw-context indexing tests.
; Build: fw-context index --build
; Uses built-in ARM GCC toolchain from ~/.platformio/packages/

[env:disco_l475vg_iot01a]
platform = ststm32
board = disco_l475vg_iot01a
framework = stm32cube
build_flags = ${common.build_flags} -MMD -MP
