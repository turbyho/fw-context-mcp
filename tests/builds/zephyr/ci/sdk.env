# Central SDK version for the Nordic Connect SDK environment.
#
# Read by ~/ncs_tools/nordic_minimal_setup.sh (the [build] activate script)
# to select which NCS toolchain to load.  Must match an installed SDK under
# ~/ncs/v<version>/.
NCS_VERSION=3.2.3
